* # учу BASH, редактирую с помощью VIM
* # потом после сохранить :wq
* # потом git add .
* # git commit -m "first commit"
* # git remote add origin https://github.com/gumirus/bash_ubuntu.git
* # git push -u origin master
